# Copy this file to .env and fill in your keys.
# This file is gitignored — never commit .env with real keys.

# OpenRouter API key (required for AI brain)
# Get one at https://openrouter.ai
OPENROUTER_API_KEY=

# OpenAI API key (optional — only if using cloud Whisper instead of local)
OPENAI_API_KEY=
